# Spotsaver Parsec CHTC Setup

> in `.sub` file, replace placeholders with your netid and your email id for notifications.

## Assumptions

* The `spotsaver-parsec-MOESI_hammer.tar.gz` file is submitted to the staging. And on untar also the folder name is `spotsaver-parsec-MOESI_hammer`.
* The `spotsaver-parsec-MOESI_hammer.tar.gz` has the gem5 already compiled. And it also has the scripts folder with contents same as automation-scripts (can check file paths in .sh script for more clarity).
* The vmlinux and parsec images are predownloaded as told in VM-CHECKPOINT-SETUP.md
